services:
  redis:
    image: redis:alpine
    container_name: redis_hotel
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes 
    restart: always

volumes:
  redis_data: